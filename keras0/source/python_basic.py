def make_incr(n):
	return lambda x: x+n

f = make_incr(13)
print(f(0))
print(f(1))

pairs = [(1, 'one'), (2, 'two'), (3, 'three'), (4, 'four')]
pairs.sort()
pairs.sort(key=lambda pair: pair[1])
print(pairs)

v = [2,3,4]
",".join() = lambda v: str(v))
			

		